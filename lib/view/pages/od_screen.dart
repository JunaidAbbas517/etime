import 'package:flutter/material.dart';

class OdScreen extends StatelessWidget {
  const OdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('OD'),
      ),
    );
  }
}
